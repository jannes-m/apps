This `App` calculates for a given gear ratio the optimal chain- and cogring teeth combination in terms of **skid patches** . Only results within the sprecified tolerance range are kept, and sorted descendingly by the number of skid patches.
